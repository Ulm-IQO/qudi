#  Example Qudi configuration file.
#
#
#
# IMPORTANT: The format of every 'module.Class' line has changed on 28.6.2015.
# =========  The class name now needs to be explicitly given here in the config.
#		  the config line must the form package.module.Class.ClassName
global:
    # list of modules to load when starting
    startup: ['man', 'tray']

    module_server:
        address: 'localhost'
        port: 12345

    ## For controlling the appearance of the GUI:
    stylesheet: 'qdark.qss'

hardware:

    simpledatadummy:
        module.Class: 'simple_data_dummy.SimpleDummy'

#    picoamperemeter:
#        module.Class: 'picoamperemeter.Amperemeter'
#        interface: 'COM2'

    picoamperemeter:
        module.Class: 'amperemeter_dummy.Amperemeter'
        interface: 'COM2'

logic:

    implantercontrollerlogic:
        module.Class: 'implanter_controller_logic.ImplanterControllerLogic'
        connect:
            amperemeter: 'picoamperemeter'

    simpledatalogic:
        module.Class: 'simple_data_logic.SimpleDataLogic'
        connect:
            simpledata: 'simpledatadummy'

    fitlogic:
        module.Class: 'fit_logic.FitLogic'

    savelogic:
        module.Class: 'save_logic.SaveLogic'
        win_data_directory: 'C:/Data'   # DO NOT CHANGE THE DIRECTORY HERE! ONLY IN THE CUSTOM FILE!
        unix_data_directory: 'Data/'
        log_into_daily_directory: True

gui:
    tray:
        module.Class: 'trayicon.TrayIcon'

    man:
        module.Class: 'manager.managergui.ManagerGui'

    implantercontroller:
        module.Class: 'implanter.implantergui.ImplanterControllerLogic'
        connect:
            implanter_controller_logic: 'implantercontrollerlogic'

    simpledata:
        module.Class: 'simpledatagui.simpledatagui.SimpleDataGui'
        connect:
            simplelogic: 'simpledatalogic'
